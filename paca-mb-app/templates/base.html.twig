<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{% block title %}Site Machines{% endblock %}</title>
    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/style.css') }}">
    {% endblock %}
</head>
<body>
  <header class="site-header">
    <div class="container header-inner">
      <a class="brand" href="{{ path('home') }}">Paca Machine à bois</a>
      <nav class="main-nav">
        <a href="{{ path('home') }}">Accueil</a>
        <a href="{{ path('tool_index') }}">Outils</a>
        <a href="{{ path('contact') }}">Contact</a>
      </nav>
    </div>
  </header>

  <main class="container" id="main">
    {% for type, messages in app.flashes %}
      <div class="flash flash-{{ type }}">
        {% for msg in messages %}{{ msg }}{% endfor %}
      </div>
    {% endfor %}

    {% block body %}{% endblock %}
  </main>

  <footer class="site-footer">
    <div class="container">
      <div class="footer-content">
        <p>© {{ "now"|date("Y") }} Machines à Bois - Prototype pédagogique</p>
        <p>Adresse: 123 Rue des Artisans, 75000 Paris, France</p>
        <p>Téléphone: +33 1 23 45 67 89</p>
        <p>Email: mail@mail.fr</p>
      </div>
      <div class="footer-content">
        <a href="{{ path('home') }}">Accueil</a>
        <a href="{{ path('tool_index') }}">Outils</a>
        <a href="{{ path('contact') }}">Contact</a>
        <a href="#">Mentions légales</a>
        <a href="#">Politique de confidentialité</a>
      </div>
    </div>
    <div class="koji">
      <a href="https://koji-dev.fr" target="_blank" rel="noopener">
        <span>Site web créé avec Koji</span>
      </a>
    </div>
  </footer>
</body>
</html>
